{
    "require": {
        "twig/twig": "^2.5",
        "matthiasmullie/minify": "^1.3",
        "symfony/polyfill-mbstring": "1.20.0"
    },
    "scripts": {
        "publish": "rm -Rf .git .gitignore; npm run dev; rm -Rf node_modules"
    }
}
